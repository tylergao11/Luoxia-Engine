# 位置说明

本目录是 **Engine 仓库内的设计树镜像**（便于会话审计看到 diff），内容与兄弟根 `Luoxia-Unity/` 同步。

- **未来正式 Host 根**：`join(parent(Luoxia-Engine), "Luoxia-Unity")`（与 Engine 并列，无盘符写死）
- **本镜像**：仅设计产物；不是 Unity Project；不修改 contracts

若两边不一致，以兄弟 `Luoxia-Unity/` 为准，并再同步回本目录。